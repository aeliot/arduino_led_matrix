#ifndef 8x8Mat_h
#define 8x8Mat_h

#include "Arduino.h"


class Display
{
 public:
  //enum Color { RED, GREEN, YELLOW };
  Display(int clock, int data, int latch);
  void print();
  void setChar(char dispChar);
  byte imageArray[2][8];
  void clear();
 private:
  int clockPin;
  int dataPin;
  int latchPin;
};


#endif
